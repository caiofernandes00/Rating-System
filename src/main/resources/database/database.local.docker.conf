exposed {
    postgres {
        host = "rating_system_postgres",
        port = 5432,
        database = "rates",
        user = "postgres",
        driver = "org.h2.Driver",
        password = "postgres"
    }
}
